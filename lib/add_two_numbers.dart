library add_two_numbers;

/// A Calculator class that provides a method to add two numbers.
class Calculator {
  /// Returns the sum of [a] and [b].
  int addTwoNumbers(int a, int b) {
    return a + b;
  }
}
